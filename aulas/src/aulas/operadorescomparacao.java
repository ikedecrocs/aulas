package aulas;

public class operadorescomparacao {

	public static void main(String[] args) {
	//comparação ordinal
	//compara tipos primitivos númericos e o tipo char
		boolean b = (10/3);
		boolean w = (x<=y);
		if(x>=y) {}
		
	//operador instanceof
	//compara o tipo da classe de uma referencia de um objeto
		String str = "Uma String";
		if( str instanceof String) {} //true
		if( srt instanceof Object) {} //true
		
	//comparação de igualdade
	//comparam tipos primitivos,valores literais e referencia de objetos
		if(abc==10) {}
		boolean b = (xyz!=50);
		if(refobj1==refobj2) {}
	}

}
